import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    night: {
      white: '#FFF',
      black: '#000',
      blue: '#0A1128',
      violet: '#7F5A83',
      green: '#077FDE',
      red: '#077FDE',
      gray: '#C6CAED',
    },
  },
});

export default theme;
